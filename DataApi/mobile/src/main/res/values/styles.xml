<resources>

    <!-- Base application theme. -->
    <style name="MorenoTheme" parent="android:Theme.DeviceDefault.Light">
        <item name="android:windowBackground">@drawable/background</item>
    </style>

</resources>
